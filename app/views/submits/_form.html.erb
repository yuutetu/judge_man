<%= form_for(@submit) do |f| %>
  <% if @submit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submit.errors.count, "error") %> prohibited this submit from being saved:</h2>

      <ul>
      <% @submit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :select_item %><br />
    <%= f.text_field :select_item %>
  </div>
  <div class="field">
    <%= f.label :judge %><br />
    <%= f.text_field :judge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
